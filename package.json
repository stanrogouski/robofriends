{
  "name": "robofriends",
  "homepage": "https://stanrogouski.github.io/robofriends/",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "caliper": "^0.2.2",
    "node": "^17.4.0",
    "react": "^16.13.0",
    "react-dom": "^16.13.0",
    "react-redux": "^8.0.0",
    "react-scripts": "^3.0.1",
    "redux": "^4.1.2",
    "tachyons": "^4.12.0"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "start": "react-scripts --openssl-legacy-provider start",
    "build": "react-scripts --openssl-legacy-provider build"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^3.2.3"
  }
}
